# ============================================================
# Lunexa - Streamlit Cloud requirements
# Python 3.11 (pinned via .python-version)
# ============================================================

# --- UI ---
streamlit>=1.28.0,<2.0.0
streamlit-option-menu>=0.3.6

# --- Numerics (must install BEFORE pyradiomics so its build has numpy) ---
numpy==1.26.4

# --- Deep learning (CPU wheel, no CUDA needed) ---
torch==2.2.2
torchvision==0.17.2

# --- Medical imaging ---
monai==1.3.0
SimpleITK==2.3.1

# --- Radiomics ---
# PyPI tarball for 3.1.0 is broken (ships 3.0.1a1 internally, missing cmatrices.h).
# Install directly from the official GitHub tag where the full C source is intact.
pyradiomics @ git+https://github.com/AIM-Harvard/pyradiomics.git@v3.1.0

# --- RT structures ---
rt-utils==1.2.7

# --- Utilities ---
matplotlib==3.8.4
scikit-learn==1.4.2
Pillow>=10.0.0