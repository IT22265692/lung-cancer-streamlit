# Core app
streamlit>=1.28.0,<2.0.0
streamlit-option-menu>=0.3.6
 
# Deep learning — CPU wheel, keeps install fast and small on Streamlit Cloud
# torch 2.2.x has a stable cp311 manylinux wheel; avoids source builds
torch==2.2.2
torchvision==0.17.2
 
# Medical imaging
monai==1.3.0
SimpleITK==2.3.1
 
# Radiomics — 3.1.0 is the last release with a pre-built cp311 wheel
# Do NOT leave unpinned; PyPI will try the dev tarball which fails on Python 3.14
pyradiomics==3.1.0
 
# RT structures
rt-utils==1.2.7
 
# Numerics / viz
numpy==1.26.4
matplotlib==3.8.4
scikit-learn==1.4.2
Pillow>=10.0.0
 